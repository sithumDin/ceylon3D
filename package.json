{
  "name": "ceylon3d-orchestrator",
  "private": true,
  "scripts": {
    "setup:all": "npm --prefix frontend install && npm --prefix ecommerce install",
    "dev:backend": "mvn -f itp-ecommerce/pom.xml -Dmaven.test.skip=true spring-boot:run",
    "dev:frontend": "npm --prefix frontend run dev -- --port 3000",
    "dev:shop": "npm --prefix ecommerce run dev -- --port 5175",
    "dev:all": "concurrently -n BACKEND,FRONTEND,SHOP -c yellow,cyan,green \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:shop\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}